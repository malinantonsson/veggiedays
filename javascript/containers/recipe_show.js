import React, { Component } from 'react';
import { connect } from 'react-redux';

export default class RecipeShow extends Component {
  render() {
    return (
      <div>Show a recipe</div>
    );
  }
}
